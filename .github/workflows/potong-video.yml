name: Process and Cut Video

on:
  workflow_dispatch:
    inputs:
      video_url:
        required: false
        default: ''
      timestamps:
        required: false
        default: ''
      judul:
        required: false
        default: ''
      deskripsi:
        required: false
        default: ''
      tag:
        required: false
        default: ''
      channel:
        required: false
        default: ''
      lokasi:
        required: false
        default: ''
      penggunaan_ai:
        required: false
        default: ''
      pembatasan_usia:
        required: false
        default: ''
      pilih_audiens:
        required: false
        default: ''
      tanggal_bulan_tahun:
        required: false
        default: ''
      jam_tayang:
        required: false
        default: ''

jobs:
  build-and-process:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Debug Inputs
        run: |
          echo "VIDEO_URL=${{ github.event.inputs.video_url }}"
          echo "TIMESTAMPS=${{ github.event.inputs.timestamps }}"
          echo "CHANNEL=${{ github.event.inputs.channel }}"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install FFmpeg, yt-dlp & requests
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          pip install -U yt-dlp requests

      - name: Create cookies.txt
        run: |
          cat > cookies.txt << 'EOF'
          ${{ secrets.YT_COOKIES }}
          EOF

      - name: Run Video Processing Script
        id: process_video
        env:
          VIDEO_URL: ${{ github.event.inputs.video_url }}
          TIMESTAMPS: ${{ github.event.inputs.timestamps }}
        run: |
          python << 'PYTHON'
          import os
          import sys
          import glob
          import json
          import requests

          video_url = os.environ.get("VIDEO_URL", "").strip()
          timestamps_raw = os.environ.get("TIMESTAMPS", "").strip()

          if not video_url:
              print("⚠️ URL Video kosong. Melewati proses download & potong.")
              with open(os.environ["GITHUB_OUTPUT"], "a") as f:
                  f.write("skip_upload=true\n")
              sys.exit(0)

          print("Download target:", video_url)

          download_success = False

          # METODE 1: Cobalah yt-dlp dengan client android_vr / web_embedded
          cookie_flag = "--cookies cookies.txt" if os.path.exists("cookies.txt") and os.path.getsize("cookies.txt") > 10 else ""
          
          cmd_ytdlp = (
              f'yt-dlp '
              f'{cookie_flag} '
              f'--extractor-args "youtube:player_client=android_vr,web_embedded" '
              f'--recode-video mp4 '
              f'"{video_url}" '
              f'-o "video_mentah.%(ext)s"'
          )
          
          print("Mengunduh via yt-dlp...")
          if os.system(cmd_ytdlp) == 0:
              download_success = True

          # METODE 2: Fallback ke Cobalt API jika yt-dlp diblokir YouTube
          if not download_success:
              print("⚠️ yt-dlp terkena blokir bot YouTube. Mengalihkan ke Cobalt API Service...")
              try:
                  headers = {
                      "Accept": "application/json",
                      "Content-Type": "application/json"
                  }
                  payload = {
                      "url": video_url,
                      "videoQuality": "max"
                  }
                  response = requests.post("https://api.cobalt.tools/", headers=headers, json=payload, timeout=30)
                  res_data = response.json()

                  direct_url = res_data.get("url")
                  if direct_url:
                      print("Dapat URL langsung dari Cobalt API. Mengunduh file video...")
                      r = requests.get(direct_url, stream=True)
                      with open("video_mentah.mp4", "wb") as f:
                          for chunk in r.iter_content(chunk_size=1024*1024):
                              if chunk:
                                  f.write(chunk)
                      download_success = True
                      print("Download via Cobalt API Sukses!")
                  else:
                      print("Cobalt Response Error:", res_data)
              except Exception as e:
                  print("Gagal via Cobalt API:", str(e))

          files = glob.glob("video_mentah.*")
          if not download_success or not files:
              print("❌ Semua metode download gagal. Hentikan proses.")
              sys.exit(1)

          source = files[0]
          print("Downloaded file:", source)

          # PROSES POTONG VIDEO DENGAN FFMPEG
          if timestamps_raw:
              print("Memotong video dengan timestamps:", timestamps_raw)
              parts = timestamps_raw.split(",")
              filter_complex = ""
              concat_inputs = ""

              for i, item in enumerate(parts):
                  start, end = item.strip().split("-")
                  filter_complex += (
                      f"[0:v]trim=start={start}:end={end},setpts=PTS-STARTPTS[v{i}];"
                      f"[0:a]atrim=start={start}:end={end},asetpts=PTS-STARTPTS[a{i}];"
                  )
                  concat_inputs += f"[v{i}][a{i}]"

              filter_complex += (
                  f"{concat_inputs}concat=n={len(parts)}:v=1:a=1[outv][outa]"
              )

              ffmpeg_cmd = (
                  f'ffmpeg -y '
                  f'-i "{source}" '
                  f'-filter_complex "{filter_complex}" '
                  f'-map "[outv]" '
                  f'-map "[outa]" '
                  f'-c:v libx264 '
                  f'-c:a aac '
                  f'video.mp4'
              )

              print("Running FFmpeg...")
              if os.system(ffmpeg_cmd) != 0:
                  print("FFmpeg gagal memotong video.")
                  sys.exit(1)

          else:
              os.rename(source, "video.mp4")

          print("Proses download dan pemotongan selesai!")
          PYTHON

      - name: Setup Node.js
        if: steps.process_video.outputs.skip_upload != 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Node Dependencies
        if: steps.process_video.outputs.skip_upload != 'true'
        run: |
          npm install googleapis

      - name: Upload to YouTube via Node.js
        if: steps.process_video.outputs.skip_upload != 'true'
        env:
          CLIENT_ID: ${{ secrets[format('YOUTUBE_CLIENT_ID_f044_{0}', github.event.inputs.channel)] }}
          CLIENT_SECRET: ${{ secrets[format('YOUTUBE_CLIENT_SECRET_f044_{0}', github.event.inputs.channel)] }}
          REFRESH_TOKEN: ${{ secrets[format('YOUTUBE_REFRESH_TOKEN_f044_{0}', github.event.inputs.channel)] }}

          VIDEO_TITLE: ${{ github.event.inputs.judul }}
          VIDEO_DESCRIPTION: ${{ github.event.inputs.deskripsi }}
          PUBLISH_DATE: ${{ github.event.inputs.tanggal_bulan_tahun }}
          VIDEO_TIME: ${{ github.event.inputs.jam_tayang }}
          LOCATION: ${{ github.event.inputs.lokasi }}
          TAGS: ${{ github.event.inputs.tag }}
          AI_CONTENT: ${{ github.event.inputs.penggunaan_ai }}
          AUDIENCE: ${{ github.event.inputs.pilih_audiens }}
          AGE_RESTRICTION: ${{ github.event.inputs.pembatasan_usia }}
          THUMBNAIL_ID: ""

        run: |
          node upload.js
